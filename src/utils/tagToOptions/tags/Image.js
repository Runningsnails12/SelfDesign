/** @type {import("..").ComponentOptions} */
const options = {
  width: ['string', 0],
  height: ['string', 0],
  border: ['string', 0],
  'box-shadow': 'string',
};

const values = {
  url: 'string'
};

/** @type {import("..").Result} */
export default {
  options,
  values
};